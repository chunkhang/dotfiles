- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.ackrc:
    ~/.dotfiles: ''
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.iterm2:
    ~/.jshintrc:
    ~/.pip:
    ~/.tmux.conf:
    ~/.ubersicht:
    ~/.vim:
    ~/.vimrc:
    ~/.zsh:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
