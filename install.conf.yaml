- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.alacritty.yml:
    ~/.bash_profile:
    ~/.config/khal:
    ~/.config/nvim:
    ~/.config/ranger:
    ~/.ctags.d:
    ~/.digrc:
    ~/.fdignore:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.hammerspoon:
    ~/.hushlogin:
    ~/.mbsyncrc:
    ~/.mime.types:
    ~/.mongorc.js:
    ~/.msmtprc:
    ~/.mutt:
    ~/.newsboat:
    ~/.patat.yaml:
    ~/.peco:
    ~/.pip:
    ~/.ripgreprc:
    ~/.scripts:
    ~/.skhdrc:
    ~/.tmux.conf:
    ~/.uebersicht:
    ~/.vdirsyncer:
    ~/.weechat:
    ~/.yabairc:
    ~/.zprofile:
    ~/.zsh:
    ~/.zshenv:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
