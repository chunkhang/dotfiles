- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bash_profile:
    ~/.chunkwmrc:
    ~/.ctags.d:
    ~/.eslintrc.js:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.hushlogin:
    ~/.jshintrc:
    ~/.peco:
    ~/.pip:
    ~/.ripgreprc:
    ~/.scripts:
    ~/.skhdrc:
    ~/.tmux:
    ~/.tmux.conf:
    ~/.ubersicht:
    ~/.vim:
    ~/.vimrc:
    ~/.weechat:
    ~/.zprofile:
    ~/.zsh:
    ~/.zshenv:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
