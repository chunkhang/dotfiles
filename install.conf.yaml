- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.ackrc:
    ~/.chunkwmrc:
    ~/.ctags.d:
    ~/.dotfiles: ''
    ~/.eslintrc.json:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.jshintrc:
    ~/.pip:
    ~/.skhdrc:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.weechat:
    ~/.zsh:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
