- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.ackrc:
    ~/.chunkwmrc:
    ~/.dotfiles: ''
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.iterm2:
    ~/.jshintrc:
    ~/.pip:
    ~/.skhdrc:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.zsh:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
