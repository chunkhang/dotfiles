- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bash_profile:
    ~/.chunkwmrc:
    ~/.ctags.d:
    ~/.eslintrc.json:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.jshintrc:
    ~/.peco:
    ~/.pip:
    ~/.ripgreprc:
    ~/.scripts:
    ~/.skhdrc:
    ~/.tmux:
    ~/.tmux.conf:
    ~/.ubersicht:
    ~/.vim:
    ~/.vimrc:
    ~/.weechat:
    ~/.zlogin:
    ~/.zsh:
    ~/.zshenv:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
