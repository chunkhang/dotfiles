- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bash_profile:
    ~/.ctags.d:
    ~/.eslintrc.js:
    ~/.fdignore:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.hushlogin:
    ~/.jshintrc:
    ~/.mbsyncrc:
    ~/.mime.types:
    ~/.msmtprc:
    ~/.mutt:
    ~/.peco:
    ~/.pip:
    ~/.ripgreprc:
    ~/.scripts:
    ~/.skhdrc:
    ~/.tmux.conf:
    ~/.ubersicht:
    ~/.vim:
    ~/.vimrc:
    ~/.weechat:
    ~/.yabairc:
    ~/.zprofile:
    ~/.zsh:
    ~/.zshenv:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
