#!/bin/sh

output=$( \
  tmux list-sessions -F '(#{session_id}) #{session_name}' | sort | \
  fzf --tiebreak=index --no-sort
)

if [[ "$?" == 0 ]]; then
  target_session=$(echo "$output" | cut -d ' ' -f 1)
  tmux switch-client -t "$target_session"
fi
