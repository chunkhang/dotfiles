const colors = {
  white: '#abb2bf',
  grey: '#5c6370',
  red: '#e06c75',
};

const paddings = {
  x: 8,
  y: 8,
};

const base = {
  fontFamily: 'Iosevka Extended',
  fontSize: 12,
  color: colors.white,
};

const theme = {
  base,
  colors,
  paddings,
};

export default theme;
