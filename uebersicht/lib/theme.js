const colors = {
  white: '#abb2bf',
  grey: '#5c6370',
  red: '#e06c75',
};

const paddings = {
  x: 15,
  y: 8,
};

const elements = {
  divider: '|',
};

const base = {
  fontFamily: 'Iosevka Extended',
  fontSize: 12,
  color: colors.white,
};

const theme = {
  base,
  colors,
  paddings,
  elements,
};

export default theme;
